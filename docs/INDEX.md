# Project Structure

- `docs/` documentation and guides
- `scripts/` automation and report tools
- `src/` future code or extensions
- `.devcontainer/` dev container config
- `.vscode/` editor settings, tasks, keybindings
- `reports/` generated reports (ignored by git)
